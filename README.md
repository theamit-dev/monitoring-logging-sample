# monitoring-logging-sample

This is sample application to demonstrate logging, messaging format and exception handling


Clone the repo to your local machine (needs python3.10)

```bash 

python -m uvicorn main:app --reload 

```